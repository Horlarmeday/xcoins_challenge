export enum StatusCodes {
    CREATED = 201,
    OK = 200,
    BAD_REQUEST = 400,
    UNAUTHORIZED = 401,
    FORBIDDEN = 403,
    NOT_FOUND = 404,
    TIMED_OUT = 408,
    SERVER_ERROR = 500,
    SERVICE_UNAVAILABLE = 501,
}